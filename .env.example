# Joshua Authentication Service - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# ADMIN CONFIGURATION (Required for first admin user creation)
# =============================================================================

# First administrator account credentials
# These will be used to create the initial admin user if no admin exists
VK_ADMIN_USERNAME=your_admin_username
VK_ADMIN_PASSWORD=your_secure_admin_password_here
VK_ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# SECURITY CONFIGURATION (Critical)
# =============================================================================

# Secret key for session signing and encryption
# Generate a secure key with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
# WARNING: Changing this key will invalidate all existing user sessions
VK_SECRET_KEY=generate_a_secure_64_character_key_here

# Session expiration time in hours (default: 24)
VK_SESSION_EXPIRE_HOURS=24

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Path to SQLite database file inside container
VK_DB_PATH=/data/joshua.db

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate a secure secret key:
#    python3 -c "import secrets; print(secrets.token_urlsafe(64))"
#
# 3. Edit .env with your actual values (especially admin credentials and secret key)
#
# 4. Start the service:
#    docker-compose up -d
#
# 5. Access admin panel at: https://auth.yourdomain.fr
#
# =============================================================================
# SECURITY NOTES
# =============================================================================

# - Never commit the .env file to version control
# - Use strong passwords for admin accounts
# - Generate a unique secret key for each installation
# - Regularly rotate the secret key in production
# - Monitor admin access logs
#
# For more information, see: https://github.com/yourrepo/joshua-auth